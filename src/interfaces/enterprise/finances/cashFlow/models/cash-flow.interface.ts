
export interface CashFlowFullData {
    total: number,
    currentDate: number,
    currentMonth: number,
    currentYear: number,
    percentages: {
        currentDate: number,
        currentMonth: number,
        currentYear: number
    }
}