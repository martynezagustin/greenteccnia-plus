import { DateDevicePipe } from './date-device.pipe';

describe('DateDevicePipe', () => {
  it('create an instance', () => {
    const pipe = new DateDevicePipe();
    expect(pipe).toBeTruthy();
  });
});
